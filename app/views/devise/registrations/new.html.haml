.devise-wrapper
  .devise-wrapper__contents
    .devise-wrapper__contents__header
      %h2 新規登録
    = form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f|
      = render "devise/shared/error_messages", resource: resource
      .devise-wrapper__contents__content
        .field
          = f.label :name, "名前", class: "default-label"
          = f.text_field :name, autofocus: true, autocomplete: "name", class: "default-text"
        .field
          = f.label :email, "メールアドレス", class: "default-label"
          = f.email_field :email, autofocus: true, autocomplete: "email", class: "default-text"
        .field
          = f.label :password, "パスワード", class: "default-label", class: "default-label"
          - if @minimum_password_length
            (#{@minimum_password_length} 文字以上)
          = f.password_field :password, autocomplete: "new-password", class: "default-text"
        .field
          = f.label :password_confirmation, "パスワード（確認用）", class: "default-label"
          = f.password_field :password_confirmation, autocomplete: "new-password", class: "default-text"
        .actions
          //= f.submit "新規登録"
          = button_tag "新規登録", type: 'submit', class: "btn-form"
    .devise-wrapper__contents__footer
      = render "devise/shared/links"
